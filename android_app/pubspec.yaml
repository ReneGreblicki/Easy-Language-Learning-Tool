name: easy_language_flashcards
description: Offline Android companion for Easy Language Learning Tool.
publish_to: none
version: 0.1.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  just_audio: ^0.10.5
  path: ^1.9.1
  path_provider: ^2.1.5
  sqflite: ^2.4.2
  supabase_flutter: ^2.10.3
  uuid: ^4.5.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
